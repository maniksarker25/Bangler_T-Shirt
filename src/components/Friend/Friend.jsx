import React from 'react';

const Friend = ({ring}) => {
    return (
        <div>
            <h1>Friend</h1>
            <p><small>Ring:{ring}</small></p>
        </div>
    );
};

export default Friend;